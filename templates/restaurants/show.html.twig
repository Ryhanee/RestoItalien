{% extends 'base.html.twig' %}
{%  block title %}
{% endblock %}

{% block body %}

 <div class="has-text-centered-touch resto-title" style="background-image: url({{ restos.image }});">
     <div class="container">
         <h1>{{ restos.libelle }}</h1>
     </div>


 </div>
<div class="container">
    <h2>Menu</h2>
    <ul>
    {% for menu in restos.menus %}
    <li>
        <ul>
            {% for plat in menu.plats %}
            <li>
                {{ plat.libelle }}
            </li>
            {% endfor %}
        </ul>
        </li>

    {% endfor %}
    </ul>


</div>

{% endblock %}
